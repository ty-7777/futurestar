<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.AiSessionMapper">
     <insert id="createSession"  useGeneratedKeys="true" keyProperty="session.id">
          INSERT INTO ai_conversation_session
          (user_id, session_name)
          VALUES (#{userId}, #{session.sessionName})
     </insert>
     <select id="sessionList"  resultType="com.situ.futurestar.core.vo.AiConversationSessionVO">
          SELECT id , session_name, create_time
          FROM ai_conversation_session
          WHERE user_id = #{userId}
          AND deleted = 0
          ORDER BY create_time desc
     </select>
     <update id="setSessionName">
          UPDATE ai_conversation_session
          SET session_name = #{sessionName}
          WHERE id = #{sessionId}
          AND deleted = 0
     </update>
     <select id="selectById" resultType="com.situ.futurestar.core.entity.AiConversationSession">
          SELECT * FROM ai_conversation_session
          WHERE  id = #{sessionId}
          AND deleted = 0
     </select>
     <update id="deleteSession">
          UPDATE ai_conversation_session
          SET deleted = 1
          WHERE id = #{sessionId}
          AND user_id = #{userId}
          AND deleted = 0
     </update>
</mapper>
