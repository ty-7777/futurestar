<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.AssessmentMapper">
     <select id="getList" resultType="com.situ.futurestar.core.entity.Questionnaire">
         SELECT * FROM questionnaire
         WHERE deleted = 0
         AND status = 'PUBLISHED'
     </select>
    <select id="getQuestions" resultType="com.situ.futurestar.core.entity.Question">
        SELECT * FROM question
        WHERE questionnaire_id = #{id}
        AND deleted = 0
        ORDER BY sort_order
    </select>
    <insert id="saveAssessment" parameterType="com.situ.futurestar.core.entity.AssessmentResult"
    useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assessment_result
            (user_id, questionnaire_id, answers, ai_score, ai_suggestion)
        VALUES (#{userId}, #{questionnaireId}, #{answers}, #{aiScore}, #{aiSuggestion})
    </insert>
    <select id="selectByUserId" resultType="com.situ.futurestar.core.entity.AssessmentResult">
        SELECT *
        FROM assessment_result
        WHERE user_id = #{userId}
        AND deleted = 0
    </select>
    <select id="selectById" resultType="com.situ.futurestar.core.entity.AssessmentResult">
        SELECT *
        FROM assessment_result
        WHERE id = #{id}
        AND deleted = 0
    </select>

    <!-- ================= 管理端 ================= -->
    <select id="listAllQuestionnaires" resultType="com.situ.futurestar.core.entity.Questionnaire">
        SELECT * FROM questionnaire
        WHERE deleted = 0
        ORDER BY create_time DESC
    </select>
    <select id="getQuestionnaireById" resultType="com.situ.futurestar.core.entity.Questionnaire">
        SELECT * FROM questionnaire
        WHERE id = #{id}
        AND deleted = 0
    </select>
    <insert id="insertQuestionnaire" parameterType="com.situ.futurestar.core.entity.Questionnaire"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO questionnaire
            (title, description, status)
        VALUES (#{title}, #{description}, #{status})
    </insert>
    <update id="updateQuestionnaire" parameterType="com.situ.futurestar.core.entity.Questionnaire">
        UPDATE questionnaire
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null and status != ''">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>
    <update id="deleteQuestionnaire">
        UPDATE questionnaire
        SET deleted = 1
        WHERE id = #{id} AND deleted = 0
    </update>
    <update id="updateQuestionnaireStatus">
        UPDATE questionnaire
        SET status = #{status}
        WHERE id = #{id} AND deleted = 0
    </update>
    <insert id="insertQuestion" parameterType="com.situ.futurestar.core.entity.Question"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question
            (questionnaire_id, content, type, options, sort_order)
        VALUES (#{questionnaireId}, #{content}, #{type}, #{options}, #{sortOrder})
    </insert>
    <update id="updateQuestion" parameterType="com.situ.futurestar.core.entity.Question">
        UPDATE question
        <set>
            <if test="content != null">content = #{content},</if>
            <if test="type != null and type != ''">type = #{type},</if>
            <if test="options != null">options = #{options},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>
    <update id="deleteQuestion">
        UPDATE question
        SET deleted = 1
        WHERE id = #{id} AND deleted = 0
    </update>
</mapper>