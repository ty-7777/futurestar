<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.ConfigMapper">
    <select id="listAll" resultType="com.situ.futurestar.core.entity.SysConfig">
        SELECT * FROM sys_config
        WHERE deleted = 0
        ORDER BY id
    </select>
    <select id="getByKey" resultType="com.situ.futurestar.core.entity.SysConfig">
        SELECT * FROM sys_config
        WHERE config_key = #{key}
          AND deleted = 0
    </select>
    <update id="updateValue">
        UPDATE sys_config
        SET config_value = #{configValue}
        WHERE config_key = #{key}
          AND deleted = 0
    </update>
</mapper>
