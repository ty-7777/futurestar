<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.CourseMapper">
    <select id="list" resultType="com.situ.futurestar.core.entity.CoursePackage">
        SELECT * FROM course_package
        WHERE status = 'ENABLED'
        AND deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR coach_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>
    <select id="getPackageById" resultType="com.situ.futurestar.core.entity.CoursePackage">
        SELECT * FROM course_package
        WHERE  id = #{id}
          AND status = 'ENABLED'
          AND deleted = 0
    </select>
    <select id="getSlots" resultType="com.situ.futurestar.core.entity.CourseSlot">
         SELECT * FROM course_slot
         WHERE package_id = #{id}
         AND  course_date = #{date}
         AND  status = 'AVAILABLE'
         AND  deleted = 0
    </select>
    <select id="selectBySlotId" resultType="com.situ.futurestar.core.entity.CourseSlot">
        SELECT * FROM course_slot
        WHERE  id = #{id}
        AND  deleted = 0
    </select>
    <select id="selectMyAppointment" resultType="com.situ.futurestar.core.vo.CourseAppointmentVO">
        SELECT
            a.id, a.user_id, a.package_id, a.slot_id, a.status, a.report_url, a.create_time,
            p.name AS package_name, p.coach_name,
            s.course_date, s.time_range
        FROM course_appointment a
                 LEFT JOIN course_package p ON p.id = a.package_id
                 LEFT JOIN course_slot     s ON s.id = a.slot_id
        WHERE a.user_id = #{userId}
          AND a.status = #{status}
          AND a.deleted = 0
    </select>
    <update id="plusCurrentCount">
        UPDATE course_slot
        SET current_count = current_count + 1,
            status = IF(current_count >= max_count, 'FULL', 'AVAILABLE')
        WHERE id = #{id}
          AND current_count &lt; max_count
          AND status = 'AVAILABLE'
          AND deleted = 0
    </update>
    <insert id="createAppointment">
        INSERT INTO course_appointment
        ( user_id, slot_id, package_id, active_key)
        VALUES (#{userId}, #{slotId}, #{packageId}, 1)
    </insert>
    <!-- 防重复预约预检查：同一用户对同一时段是否已有有效（未取消）预约 -->
    <select id="countActiveByUserSlot" resultType="int">
        SELECT COUNT(*) FROM course_appointment
        WHERE user_id = #{userId}
          AND slot_id = #{slotId}
          AND status != 'CANCELED'
          AND deleted = 0
    </select>
    <select id="selectByAppointmentId" resultType="com.situ.futurestar.core.vo.CourseAppointmentVO">
        SELECT
            a.id, a.user_id, a.package_id, a.slot_id, a.status, a.report_url, a.create_time,
            p.name AS package_name, p.coach_name, p.price,
            s.course_date, s.time_range
        FROM course_appointment a
                 LEFT JOIN course_package p ON p.id = a.package_id
                 LEFT JOIN course_slot     s ON s.id = a.slot_id
        WHERE a.id = #{id}
          AND a.deleted = 0
    </select>
    <update id="updateAppointmentStatus">
        UPDATE course_appointment
        SET status = 'CANCELED',
            active_key = NULL
        WHERE id = #{id}
        AND status IN ('PENDING', 'CONFIRMED')
        AND deleted = 0
    </update>
    <update id="decreaseCurrentCount">
        UPDATE course_appointment a
            LEFT JOIN course_slot s ON s.id = a.slot_id
            SET s.current_count = s.current_count - 1,
                s.status = IF(s.current_count &lt; s.max_count, 'AVAILABLE', 'FULL')
        WHERE a.id = #{id} AND s.current_count &gt; 0
    </update>

    <!-- ================= 管理端 ================= -->
    <select id="listAll" resultType="com.situ.futurestar.core.entity.CoursePackage">
        SELECT * FROM course_package
        WHERE deleted = 0
        ORDER BY create_time DESC
    </select>
    <insert id="insertPackage" parameterType="com.situ.futurestar.core.entity.CoursePackage"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course_package
            (name, cover_url, description, price, coach_name, suitable_level, items, status)
        VALUES (#{name}, #{coverUrl}, #{description}, #{price}, #{coachName}, #{suitableLevel}, #{items}, #{status})
    </insert>
    <update id="updatePackage" parameterType="com.situ.futurestar.core.entity.CoursePackage">
        UPDATE course_package
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="coverUrl != null">cover_url = #{coverUrl},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="coachName != null">coach_name = #{coachName},</if>
            <if test="suitableLevel != null">suitable_level = #{suitableLevel},</if>
            <if test="items != null">items = #{items},</if>
            <if test="status != null and status != ''">status = #{status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>
    <update id="deletePackage">
        UPDATE course_package
        SET deleted = 1
        WHERE id = #{id} AND deleted = 0
    </update>
    <select id="listSlots" resultType="com.situ.futurestar.core.entity.CourseSlot">
        SELECT * FROM course_slot
        WHERE package_id = #{packageId}
        <if test="date != null">AND course_date = #{date}</if>
        AND deleted = 0
        ORDER BY course_date, time_range
    </select>
    <update id="updateSlot">
        UPDATE course_slot
        <set>
            <if test="dto.maxCount != null">max_count = #{dto.maxCount},</if>
            <if test="dto.status != null and dto.status != ''">status = #{dto.status},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>
    <select id="listAppointments" resultType="com.situ.futurestar.core.vo.CourseAppointmentVO">
        SELECT
            a.id, a.user_id, a.package_id, a.slot_id, a.status, a.report_url, a.create_time,
            p.name AS package_name, p.coach_name,
            s.course_date, s.time_range
        FROM course_appointment a
                 LEFT JOIN course_package p ON p.id = a.package_id
                 LEFT JOIN course_slot     s ON s.id = a.slot_id
        WHERE a.deleted = 0
        <if test="packageId != null">AND a.package_id = #{packageId}</if>
        <if test="date != null">AND s.course_date = #{date}</if>
        <if test="status != null and status != ''">AND a.status = #{status}</if>
        ORDER BY a.create_time DESC
    </select>
    <insert id="insertSlot" parameterType="com.situ.futurestar.core.entity.CourseSlot">
        INSERT INTO course_slot
            (package_id, course_date, time_range, max_count, current_count, status)
        VALUES (#{packageId}, #{courseDate}, #{timeRange}, #{maxCount}, #{currentCount}, #{status})
    </insert>
    <select id="countSlotByDate" resultType="int">
        SELECT COUNT(*) FROM course_slot
        WHERE package_id = #{packageId}
          AND course_date = #{date}
          AND deleted = 0
    </select>
    <update id="updateReportUrl">
        UPDATE course_appointment
        SET report_url = #{reportUrl}
        WHERE id = #{id} AND deleted = 0
    </update>
    <!-- 管理员确认预约（带状态守卫：仅 PENDING 可确认，防重复处理） -->
    <update id="confirmAppointment">
        UPDATE course_appointment
        SET status = 'CONFIRMED'
        WHERE id = #{id}
          AND status = 'PENDING'
          AND deleted = 0
    </update>
    <!-- 管理员拒绝预约（带状态守卫：仅 PENDING 可拒绝，防重复处理） -->
    <update id="rejectAppointment">
        UPDATE course_appointment
        SET status = 'CANCELED',
            active_key = NULL
        WHERE id = #{id}
          AND status = 'PENDING'
          AND deleted = 0
    </update>

    <!-- ================= 定时任务 ================= -->
    <update id="completeFinishedAppointments">
        UPDATE course_appointment a
            INNER JOIN course_slot s ON s.id = a.slot_id
        SET a.status = 'COMPLETED'
        WHERE a.status = 'CONFIRMED'
          AND s.course_date &lt; #{today}
          AND a.deleted = 0
    </update>
    <update id="closeExpiredSlots">
        UPDATE course_slot
        SET status = 'CLOSED'
        WHERE course_date &lt; #{today}
          AND status != 'CLOSED'
          AND deleted = 0
    </update>
    <update id="archiveOldAppointments">
        UPDATE course_appointment
        SET deleted = 1
        WHERE create_time &lt; #{before}
          AND deleted = 0
    </update>
    <select id="listAppointmentsByDate" resultType="com.situ.futurestar.core.vo.CourseAppointmentVO">
        SELECT
            a.id, a.user_id, a.package_id, a.slot_id, a.status, a.report_url, a.create_time,
            p.name AS package_name, p.coach_name,
            s.course_date, s.time_range
        FROM course_appointment a
                 LEFT JOIN course_package p ON p.id = a.package_id
                 LEFT JOIN course_slot     s ON s.id = a.slot_id
        WHERE s.course_date = #{date}
          AND a.status IN ('PENDING', 'CONFIRMED')
          AND a.deleted = 0
    </select>
</mapper>