<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.DashboardMapper">
    <select id="countPlayers" resultType="int">
        SELECT COUNT(*) FROM user
        WHERE role = 'PLAYER' AND deleted = 0
    </select>
    <select id="countTodayNewPlayers" resultType="int">
        SELECT COUNT(*) FROM user
        WHERE role = 'PLAYER' AND deleted = 0
          AND create_time &gt;= CURDATE()
    </select>
    <select id="countTodayCourseAppointments" resultType="int">
        SELECT COUNT(*) FROM course_appointment
        WHERE deleted = 0 AND create_time &gt;= CURDATE()
    </select>
    <select id="countTodayEventRegistrations" resultType="int">
        SELECT COUNT(*) FROM event_registration
        WHERE deleted = 0 AND create_time &gt;= CURDATE()
    </select>
    <select id="countPendingAppointments" resultType="int">
        SELECT COUNT(*) FROM course_appointment
        WHERE status = 'PENDING' AND deleted = 0
    </select>
</mapper>
