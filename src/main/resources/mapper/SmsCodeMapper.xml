<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.situ.futurestar.core.mapper.SmsCodeMapper">
    <select id="selectLatestValid" resultType="com.situ.futurestar.core.entity.SmsCode">
        SELECT * FROM sms_code
        WHERE phone = #{phone} AND used = 0 AND expire_time > NOW()
        ORDER BY create_time DESC LIMIT 1
    </select>
     <update id="markUsed">
         UPDATE sms_code
         SET used = 1
         WHERE id = #{id}
     </update>
     <insert id="insert" parameterType="com.situ.futurestar.core.entity.SmsCode" useGeneratedKeys="true" keyProperty="id">
         INSERT INTO sms_code (phone, code, expire_time, used, create_time)
         VALUES (#{phone}, #{code}, #{expireTime}, 0, NOW())
     </insert>
</mapper>